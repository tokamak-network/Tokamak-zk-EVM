# Tokamak zk-EVM Synthesizer Web Library

`@tokamak-zk-evm/synthesizer-web` is the browser-compatible library package for the Tokamak zk-EVM synthesizer.

It re-exports the environment-neutral synthesizer core from `../core` and is intended to be bundled into web applications without the Node-specific CLI and filesystem adapters.
