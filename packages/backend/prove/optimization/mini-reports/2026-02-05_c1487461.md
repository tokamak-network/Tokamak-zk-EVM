# Mini Report: 2026-02-05 (c1487461)

## total_wall
- 66.383591 s

## Commits
- 032ef14d Remove checkpoint logs
- e2785b42 Merge pull request #175 from tokamak-network/jake-backend-bench
- d1e06002 docs: update timing report guidance
- 8e415a71 Update config generation pipeline
- b24dbeba Add config-gen-bot docs and reorganize synthesizer docs
- cf0e3b89 Fix SLOAD origin tracing for PUSH32 keys
- b5845973 Apply review feedback for ERC20 config tooling
- ce2977b0 Merge pull request #176 from tokamak-network/jake-synth-config-bot
- 339f933f feat: add synthesizer test automation
- 2df3bf86 Split synthesizer prep/test and improve logging
- 6c7e50d3 Merge pull request #177 from tokamak-network/feat-synth-CI
- 5436eade ci: integrate synthesizer tests and add preprocess input support
- 865f9f9a chore: update task log
- f3bcceec fix: address gemini review notes
- 91ea81b1 ci: ensure test job dependencies
- 1dc830c4 ci: fix tokamak-ch compat input paths
- ca5bd45d Preserve synthesizer outputs during archive
- 5c0375b5 cli: accept permutation.json for preprocess input
- 13e41425 ci: use permutation.json for preprocess input
- eb31bffd backend: drop instance load in preprocess
- 2ff73e19 ci: update build-release preprocess input
- 23959fe7 Merge pull request #178 from tokamak-network/jake-ci-update
- d583440c feat: cache denom inverses and benchmark div_by_vanishing
- 561c4e4f chore: update backend task review
- bf2b6481 Resolve PR 179 merge conflicts
- be76316d Merge pull request #179 from tokamak-network/jake-prove-optim
- c1487461 Refresh timing report and task docs

## Change Analysis
1. **Proposition:** Preprocess loads `permutation.json` as input for generating preprocess artifacts.  
Proof (excerpt):
```text
// Load permutation (copy constraints of the variables)
let permutation_path = PathBuf::from(paths.synthesizer_path).join("permutation.json");
let permutation_raw = Permutation::read_box_from_json(permutation_path).unwrap();
// Generate preprocess
let preprocess = Preprocess::gen(&sigma, &permutation_raw, &setup_params);
```

2. **Proposition:** Prover initialization reads `permutation.json` from the synthesizer output path.  
Proof (excerpt):
```text
let permutation_path = PathBuf::from(paths.synthesizer_path).join("permutation.json");
let permutation_file_bytes = std::fs::metadata(&permutation_path)
    .map(|m| m.len() as usize)
    .unwrap_or(0);
let permutation_raw = crate::time_block!(
    "init.load.permutation",
    "load",
    vec![
        crate::timing::SizeInfo { label: "file_bytes", dims: vec![permutation_file_bytes] },
    ],
    {
        Permutation::read_box_from_json(permutation_path).unwrap()
    }
);
```

3. **Proposition:** div_by_vanishing caches denominator inverse evaluations (baseline caching).  
Proof (excerpt):
```text
let denom_y_eval_inv_slice = if let Some(idx) = cached_denom_y_index {
    cache.denom_y_eval_inv[idx].evals.as_ref()
} else {
    let evals = build_denom_inv(...);
    let mut new_cache = cache.denom_y_eval_inv.to_vec();
    new_cache.push(DenomCache { coset: xi, x_size: c, y_size: n * d, base: d, evals });
    cache.denom_y_eval_inv = new_cache.into_boxed_slice();
    cache.denom_y_eval_inv.last().unwrap().evals.as_ref()
};
```
