# Mini Report: 2026-02-05 (c1487461)

## total_wall
- 66.383591

## Commits
- 032ef14d Remove checkpoint logs
- d1e06002 docs: update timing report guidance
- 5c0375b5 cli: accept permutation.json for preprocess input
- eb31bffd backend: drop instance load in preprocess
- d583440c feat: cache denom inverses and benchmark div_by_vanishing
- 561c4e4f chore: update backend task review
- bf2b6481 Resolve PR 179 merge conflicts
- be76316d Merge pull request #179 from tokamak-network/jake-prove-optim
- c1487461 Refresh timing report and task docs

## Change Analysis
1. **Proposition:** Preprocess loads `permutation.json` as input and generates preprocess artifacts from it.
Proof (excerpt):
```text
let permutation_path = PathBuf::from(paths.synthesizer_path).join("permutation.json");
let permutation_raw = Permutation::read_box_from_json(permutation_path).unwrap();
let preprocess = Preprocess::gen(&sigma, &permutation_raw, &setup_params);
```

2. **Proposition:** Prover initialization reads `permutation.json` and records its size in the init timing span.
Proof (excerpt):
```text
let permutation_path = PathBuf::from(paths.synthesizer_path).join("permutation.json");
let permutation_file_bytes = std::fs::metadata(&permutation_path)
    .map(|m| m.len() as usize)
    .unwrap_or(0);
```

3. **Proposition:** div_by_vanishing caches denominator inverse evaluations for reuse.
Proof (excerpt):
```text
let denom_y_eval_inv_slice = if let Some(idx) = cached_denom_y_index {
    cache.denom_y_eval_inv[idx].evals.as_ref()
} else {
    let evals = build_denom_inv(...);
    let mut new_cache = cache.denom_y_eval_inv.to_vec();
    new_cache.push(DenomCache { coset: xi, x_size: c, y_size: n * d, base: d, evals });
    cache.denom_y_eval_inv = new_cache.into_boxed_slice();
    cache.denom_y_eval_inv.last().unwrap().evals.as_ref()
};
```
