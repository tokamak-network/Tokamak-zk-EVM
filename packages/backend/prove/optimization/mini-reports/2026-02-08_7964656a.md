# Mini Report: 2026-02-08 (7964656a)

## total_wall
- 27.908515

## Commits
- 3d6ce1a6 docs: update optimization reporting
- 7964656a Update backend timing and related changes

## Change Analysis
1. **Proposition:** Added explicit optimization reporting prompt with required output files and Source Series rules.
Proof (excerpt):
```text
- Main report: `prove/optimization/optimization_report.md`
- Mini-reports directory: `prove/optimization/mini-reports/`
```

2. **Proposition:** Prover initialization now computes an NTT domain size from parameters and initializes it once.
Proof (excerpt):
```text
let ntt_domain_size = ntt_domain_x
    .checked_mul(ntt_domain_y)
    .expect("2 * max(m_i, n) * 2 * s_max overflow");
init_ntt_domain_for_size(ntt_domain_size).expect("Failed to initialize NTT domain");
```

3. **Proposition:** `_biNTT` now enforces an initialized domain size and applies per-axis coset generators in NTT config.
Proof (excerpt):
```text
let expected_size = get_ntt_domain_size().expect(
    "NTT domain is not initialized. Call init_ntt_domain_for_size before using _biNTT.",
);
cfg.coset_gen = coset_gen_y.copied().unwrap_or(Self::Field::one());
```

4. **Proposition:** `div_by_vanishing_opt` now caches axis inverses and tiles them to build denominator eval inverses.
Proof (excerpt):
```text
axis_inv = cached denom_y_axis_inv or build_axis_inv(n*d, d, xi)
denom_y_eval_inv = tile_axis_inv(c, n*d, axis_inv, true)
```
