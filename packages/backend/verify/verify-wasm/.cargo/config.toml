[build]
target = "wasm32-unknown-unknown"

[target.wasm32-unknown-unknown]
rustflags = [
  "-C", "link-arg=--no-entry",
  "-C", "opt-level=3",
]

# Disable incremental compilation for WASM to avoid link issues
[profile.dev]
incremental = false

[profile.release]
incremental = false

