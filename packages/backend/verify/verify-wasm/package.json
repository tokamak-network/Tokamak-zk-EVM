{
  "name": "@tokamak-network/zkevm-verify-wasm",
  "version": "0.1.0",
  "description": "Deprecated historical WASM verifier package for Tokamak zk-EVM. Use @tokamak-zk-evm/cli for local verification or the Solidity verifier contracts for on-chain verification.",
  "keywords": [
    "tokamak",
    "zkevm",
    "snark",
    "verifier",
    "wasm",
    "zero-knowledge",
    "deprecated",
    "historical"
  ],
  "author": "Tokamak Network",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tokamak-network/Tokamak-zk-EVM"
  },
  "scripts": {
    "build": "./build.sh",
    "build:web": "wasm-pack build --target web --out-dir pkg-web",
    "build:node": "wasm-pack build --target nodejs --out-dir pkg-node",
    "build:bundler": "wasm-pack build --target bundler --out-dir pkg",
    "test": "wasm-pack test --node",
    "publish:web": "cd pkg-web && npm publish --access public",
    "publish:node": "cd pkg-node && npm publish --access public"
  }
}
