# Plan
- [ ] Confirm scope of commit (includes files outside `packages/backend`) and target remote/branch (current branch `jake-prove-optim` tracks `origin/dev`).
- [ ] Stage all requested changes (including untracked `agents/`).
- [ ] Commit with the agreed message.
- [ ] Push to the agreed remote/branch.

# Review
- [ ] Verify commit contains all expected files and no unintended omissions.
- [ ] Verify push completed successfully.
