{
  "name": "@tokamak-zk-evm/backend-wasm",
  "version": "0.0.0",
  "description": "Web-compatible Tokamak zk-EVM prover and verifier package scaffold.",
  "license": "MIT OR Apache-2.0",
  "author": "Tokamak Network",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "fixtures"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json --pretty false",
    "typecheck": "tsc -p tsconfig.json --pretty false --noEmit",
    "typecheck:scripts": "tsc --pretty false --noEmit --module NodeNext --moduleResolution NodeNext --target ES2022 --types node scripts/check-fixtures.ts scripts/convert-artifacts.ts",
    "fixtures:check": "tsx scripts/check-fixtures.ts fixtures/small/manifest.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.15.14",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
