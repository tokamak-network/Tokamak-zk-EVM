[package]
name = "backend-wasm-rkyv-decoder"
version = "0.0.0"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rkyv = { version = "0.7", features = ["alloc", "bytecheck", "validation"] }
wasm-bindgen = "0.2"
