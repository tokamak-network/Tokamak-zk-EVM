{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/playground && npm run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^8.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
