{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/frontend/synthesizer/libs/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "lerna": "^8.0.0",
    "prettier": "^3.5.3",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "pip": "^0.0.1"
  }
}
